import styled from "styled-components";
import tw from "tailwind.macro";

export const ContentContainer = styled.div`
  ${tw`flex flex-col items-center justify-center my-4 text-gray-600 text-lg leading-relaxed`}
`;
